import store from "../../redux/rooterducer";

/* var dataCard: {
    CompressId,
    resolution,
    size,
    rangeValue,
    pictureCompressUrl,
    pictureOrigineUrl 
}; */
const deleteImage = () => {
    console.log("DELETE Function");
};